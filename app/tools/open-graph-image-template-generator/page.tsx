import { PageHeader } from "@/components/page-header";

export default function OpenGraphImages() {    
    return <>
        <PageHeader heading="Open Graph Images" subheading="Generate a simple Open Graph image HTML and grab the HTML code in a few seconds." />
        <div className="flex flex-col max-w-[700px] gap-10 mt-10"></div>
    </>;
}